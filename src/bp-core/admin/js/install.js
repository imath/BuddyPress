/**
 * Bulk Install BuddyPress Plugins.
 *
 * @since 6.0.0
 */
(function( $, wp ) {

	// Trigger Install button clicks
	$( document ).ready( function() {
		console.log( wp );
	} );

}( jQuery, window.wp || {} ) );
